{
  "name": "@dev.com/orchestrator",
  "version": "3.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate": "wrangler d1 migrations apply dev-com-db",
    "db:seed": "wrangler d1 execute dev-com-db --file=./migrations/seed.sql"
  },
  "dependencies": {
    "@dev.com/shared": "workspace:*",
    "agents": "^0.0.1",
    "hono": "^4.6.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.91.0"
  }
}
